<%
/**
 * @var {function} __
 * @param {string}
 * @return {string}
 */
/**
 * @var {Array} champions
 */
%>

<div class="champions" id="champions">



  <% if (items && Array.isArray(items)) { %>

  <% items.forEach(function(item) {  %>

  <div class="col-md-3 col-xs-6 col-sm-4 col-lg-2 champion-block" data-name="<%= item.name %>">
    <div class="champion-block__inner">
      <a href="#" onclick="return false;">
        <img class="champion-block__image" ><%- item.image %>"/>
        <p class="champion-block__name"><%= item.name %></p>
      </a>
    </div>
  </div>

  <% }) %>

  <% } else {%>

  <%= __('there_no_champions') %>

  <% }%>

</div>

